servers = (
  {
    address = "<bouncer address>";
    chatnet = "Freenode";
    port = "6667";
    use_ssl = "yes";
    ssl_verify = "no";
    autoconnect = "yes";
    password = "<bouncer credentials>";
  }
);
settings = {
  core = {
    real_name = "<real name>";
    user_name = "<freenode username>";
    nick = "<freenode nick>";
  };
  "fe-text" = { actlist_sort = "refnum"; };
  "fe-common/core" = { theme = "molokai"; };
  "perl/core/scripts" = {
    awl_display_key = "$Q%K|%n$H$C$S";
    nicklist_automode = "SCREEN";
  };
};
chatnets = { Freenode = { type = "IRC"; }; };
statusbar = {
  default = {
    awl_0 = {
      items = {
        barstart = { priority = "100"; };
        awl_0 = { };
        barend = { priority = "100"; alignment = "right"; };
      };
    };
  };
};
